# BiliInsight Desktop（Bili_summary 桌面端）

B 站视频观点提炼工具的 Electron 桌面端。

## 如何手动打开
```bash
cd Bili_summary/desktop
npm install
npm run dev        # 起 Electron 窗口
# 打包：npm run dist
```

### 预期结果
弹出 Electron 桌面窗口，可操作 B 站视频提炼功能。

### 验证状态
✅ 已验证可开（build 成功，Electron 应用）
