"""数字顾问团（Vellum 深页）应用包。

注意：本文件不可删除。
site-packages 中存在一个同名的第三方 ``src`` 包（常规包），
若本目录缺少 ``__init__.py``，它会退化为命名空间包并被前者抢占，
导致 ``from src.xxx import ...`` 全部报 ModuleNotFoundError。
"""

__version__ = "1.0.0"
